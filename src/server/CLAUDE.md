# server/

> L2 | 父级: ../CLAUDE.md

成员清单
library-index.ts: TypeScript 资产索引编译器，读取 static/library YAML 真相源，通过共享资产模型规范化 single/themed 主 Logo 与 Wordmark、拒绝半套主题，并原子生成前端 SVG/分类索引，供 Admin、CLI 与 CI 复用

法则: 成员完整·一行一文件·父级链接·技术词前置

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
